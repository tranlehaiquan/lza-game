import React from 'react';

interface Props {
  className?: string;
};

const ResultSuccess: React.FC<Props> = (props) => {
  return (
    <div>
      
    </div>
  );
}

export default ResultSuccess;